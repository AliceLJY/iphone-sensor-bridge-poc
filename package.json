{
  "name": "iphone-sensor-bridge-poc",
  "version": "0.2.0",
  "private": true,
  "description": "Local phone-to-Mac file drop prototype over a browser-accessible HTTP bridge.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check test/server.test.js && bash -n scripts/install-launchd.sh",
    "test": "node --test",
    "launchd:install": "bash scripts/install-launchd.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
